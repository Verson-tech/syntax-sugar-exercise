
//inferred typing is where the compiler defines the variable's type
var answer = 4;

//ternary operator and string interpolation
var response = (answer < 9) ? $"{answer} is less than nine" : $"{answer} greater or equal to nine";

Console.WriteLine(response);